{% include 'partials/fragments/profile.html.twig' 
    with {
        id: page.header.id,
        classes: ['profile-section', page.header.color_scheme, page.header.css_classes]|join(' '),
        styles: page.header.css_styles,
        row_clss: 'auto'
    } 
%}