{% include 'partials/fragments/hero.html.twig' 
    with {
        id: page.header.id,
        classes: ['hero-section', page.header.color_scheme, page.header.css_classes]|join(' '),
        styles: page.header.css_styles,
        row_clss: 'auto',
        col_clss: 'push-center push-middle'
    } 
%}