{% include 'partials/fragments/text.html.twig' 
    with {
        id: page.header.id,
        classes: ['text-section', page.header.color_scheme, page.header.css_classes]|join(' '),
        styles: page.header.css_styles,
        row_clss: 'auto'
    } 
%}